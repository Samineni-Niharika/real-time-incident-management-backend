spring:
  profiles:
    active: dev
  application:
    name: incidentManagement
  jpa:
    hibernate:
        ddl-auto: update
  mail:
    properties:
        mail:
           smtp:
              auth: true
              starttls:
                enable: true 
server:
   port: 8080
jwt:
   expiration: 86400000